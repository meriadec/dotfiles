#!/bin/bash

options=(
  "smoke"
  "lunch"
  "not here now"
  "back in a sec"
)

( IFS=$'\n'; echo "${options[*]}" ) \
  | dmenu \
  | tti "$(mktemp --suffix .png)" \
  | xargs -I {} i3lock -i {} -b -u -f
