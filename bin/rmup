#!/bin/bash

filePath=$1
baseName=`basename $filePath`
target="/www/media/up/$baseName"

read -p "> Remove $target ? (y/n) " -n 1 -r
echo
if [[ $REPLY == "y" ]]
then
  echo "> ..."
  ssh $ZAVATTA "rm $target"
  [ $? -ne 0 ] && exit 1
  echo "> Done"
fi
